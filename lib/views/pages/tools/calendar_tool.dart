import 'package:flutter/material.dart';

class CalendarTool extends StatelessWidget {
  const CalendarTool({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: Center(child: Text("Calendar")));
  }
}
